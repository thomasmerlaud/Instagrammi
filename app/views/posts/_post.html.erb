<div id="<%= dom_id post %>">
  <p>
    <strong>Titre:</strong>
    <%= post.titre %>
  </p>

  <p>
    <strong>Description:</strong>
    <%= post.description %>
  </p>

  <% post.images.each do |images|%>
    <%= image_tag(image)  %>
  <% end  %>

</div>

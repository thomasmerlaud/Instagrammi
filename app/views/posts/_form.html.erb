
    <div style="margin-left: 10%;" class="main-content">
    <h2 style="font-size: 20px; font-weight: bold; margin-top: 20px; margin-left: 20%;">Publication</h2>
      <!-- Formulaire -->
      <div class="form-container">
        <%= form_with(model: post, html: { class: "form-container" }) do |form| %>
          <% if post.errors.any? %>
            <div style="color: red">
              <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

              <ul>
                <% post.errors.each do |error| %>
                  <li><%= error.full_message %></li>
                <% end %>
              </ul>
            </div>
          <% end %>

          <div class="form-field">
            <%= form.label :titre, "Pseudo", class: "form-label" %>
            <%= form.text_field :titre, class: "form-input", placeholder: "Ajouter un peudo - Pensez au @ !" %>
          </div>

          <div class="form-field">
            <%= form.label :description, "Description de post", class: "form-label" %>
            <%= form.text_field :description, class: "form-input", placeholder: "Ajouter une description" %>
          </div>

          <div class="form-field">
            <%= form.label :image, "URL de l'image", class: "form-label mb-2" %>
            <%= form.text_field :image, class: "form-input", placeholder: "Insérez l'URL de votre image" %>
          </div>

          <div class="form-field">
            <%= form.submit "Publier", class: "form-submit" %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
